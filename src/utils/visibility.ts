export enum Visibility {
    PUBLIC = 'Anyone',
    MEMBERS_ONLY = 'Members Only',
}
 export enum GroupPrivacy {
    PUBLIC = 'Public',
    PRIVATE = 'Private',
 }

 export enum GroupType {
    TEAM = 'Team',
    CLUB = 'Club',
 }
